version: 1.0
runtime: docker

build:
  dockerfile: Dockerfile
  context: .

run:
  network:
    port: 8081
  
  healthcheck:
    protocol: http
    path: /
    interval: 10
    timeout: 5
    healthy-threshold: 1
    unhealthy-threshold: 3